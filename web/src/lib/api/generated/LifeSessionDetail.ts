// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { LifeCounter } from "./LifeCounter";
import type { LifeEvent } from "./LifeEvent";
import type { LifeSession } from "./LifeSession";

/**
 * One tracked game in full: its header + seats, plus every recorded life change in the order
 * they happened. A session is bounded (at most [`MAX_EVENTS_PER_SESSION`] events), so this is
 * returned whole — the SPA groups `events` by `player_id` for the per-seat sparklines and
 * reads them in order for the shared timeline.
 */
export type LifeSessionDetail = { session: LifeSession, events: Array<LifeEvent>, 
/**
 * Every non-life counter that has been moved, folded out of `events` — so a client gets
 * the table's full state without re-implementing the replay. Deterministically ordered
 * (by seat, then counter, then source seat).
 */
counters: Array<LifeCounter>, };
