// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * One seat as a viewer sees it: public zones as id lists, the library as a count, the hand
 * as ids for the seat's own viewer (plus any revealed card) and a count for everyone else.
 */
export type PlaySeatSnapshot = { id: number, seat_index: number, name: string, is_host: boolean, deck_name: string | null, life: number, counters: { [key in string]: number }, commander_damage: { [key in number]: number }, out: boolean, connected: boolean, library_count: number, hand_count: number, 
/**
 * `Some` only for the viewer's own seat (the full hand, in order); for other seats it
 * lists just the cards they've revealed.
 */
hand: Array<number> | null, battlefield: Array<number>, graveyard: Array<number>, exile: Array<number>, command: Array<number>, };
